(function ($) {
  if (undefined == $.wapp)
    $.wapp = {};
  $.wapp.messages = {
    confirm : {
      title : "Are you sure ?",
      yes   : "Yes",
      no    : "No"
    },
    table : {
      nofilter : "(no filter)",
      empty    : "(empty)",
      notempty : "(not empty)",
      null     : "(null)",
      notnull  : "(not null)"
    }
  };
}(jQuery));
